---

import Check from '../assets/theme-images/icon-check.svg'
import Cube from '../assets/theme-images/icon-cube.svg'
import CubeOutline from '../assets/theme-images/icon-cube-outline.svg'

const { title, pricing } = Astro.props

---

<section class="bs-container bs-mt-lg" id="pricing">

  <!-- Header -->
  { title && <h2 class="bs-h2 text-center">{ title }</h2>}

  <!-- pricing -->
  <div class="mt-8 grid lg:grid-cols-2 gap-8 lg:gap-0">

    <!-- Item -->

    { pricing.map( ( item, index ) =>

    <article
      class:list={['@container p-6 xl:p-16 flex flex-col gap-6 h-full w-full rounded-xl overflow-hidden lg:scale-90 @xl:justify-end relative', index === 1 ? 'lg:scale-none border border-dc-blue/40 text-bs-foreground-light' : 'bg-bs-surface-0/90 border border-bs-surface-3']}
    style={ index === 1 ? 'background: linear-gradient(135deg, rgba(14,165,233,0.12) 0%, rgba(124,58,237,0.18) 100%); box-shadow: 0 0 40px rgba(14,165,233,0.15), 0 0 80px rgba(124,58,237,0.1);' : '' }>

      <!-- Content -->
      <div class="relative">

        <!-- Title -->
        <h2 class="bs-h3 flex gap-3 items-center">

          <!-- Icon -->
          { index === 1 && <Cube height="32" width="32" />  }
          { index !== 1 && <CubeOutline height="32" width="32" />  }

          <!-- Title -->
          <span>{ item.title }</span>

          <!-- Tag -->
          <div
            class="text-xs uppercase tracking-widest font-semibold hidden sm:block ms-auto rounded-full px-4 py-2 pb-[0.4rem] border border-bs-foreground-dark/30"
            set:html={ item.tag } />

        </h2>

        <!-- Intro -->
        <div class="text-xl flex-grow mt-4 " set:html={ item.content } />

        <!-- Services list -->
        <ul role="list" class="flex flex-col gap-4 my-8">

          { item.services.map( ({ item }) =>

            <li class="flex gap-2 md:gap-3 items-center">

              <!-- Check -->
              <Check height="24" width="24" />

              <!-- Item -->
              <span class="text-xl">{ item }</span>

            </li>

          )}

        </ul>

        <!-- CTA -->
        <footer class="flex justify-between items-center gap-2">

          <!-- Price -->
          desde Bs.<span class="text-4xl lg:text-5xl">{ item.price }<span class="text-2xl hidden @md:inline-block -translate-y-1 ms-2">{ item.type_payment }</span></span>

          <!-- CTA button -->
          <button type="button" class="bs-btn inline-block self-start @4xl:mt-12 text-xl" onclick="demo.showModal()">
            { item.cta }
          </button>

        </footer>

      </div>

    </article>
    )}

  </div>

</section>

